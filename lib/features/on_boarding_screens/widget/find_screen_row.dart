import 'package:flutter/cupertino.dart';

class FindScreenRow extends StatelessWidget {
  const FindScreenRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
